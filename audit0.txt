 Что было сделано:
   - Реализована система аудита (логирование действий пользователя).
   - Добавлена возможность экспорта логов (включая настройки FileProvider для Android).
   - Обновлены DI модули (AppGraph) для поддержки PathProvider и FileSharer.
   - В UI (Properties) добавлена кнопка «Export Logs» и логирование при изменении параметров.

  Сообщение коммита: feat: implement audit logging and log export functionality

Что было сделано:
   1. Локализация логов: Теперь при сохранении настроек в audit.log записываются переведенные названия экрана
      («Установки»), параметра («Сухой датчик» и т.д.) и метки значения («Задано»).
   2. Ресурсы: Добавлен новый строковый ресурс audit_set_value («Задано» / «Set»).
   3. ViewModel: Метод send() в SetPropViewModel теперь поддерживает передачу локализованных строк для аудита.
   4. UI: SetPropDialog передает текущие переводы из ресурсов в логику сохранения.